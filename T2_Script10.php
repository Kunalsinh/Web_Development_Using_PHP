<?php
    // finding area of rectangle and the formula is (area = length * width) 
    $width = 20;
    $length = 30;
    $area;

    $area = $width * $length;

    echo "Area of Rectangle is $area."

?>